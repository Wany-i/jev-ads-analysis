# 变更记录

## [Unreleased]

## [0.2.0] — 2026-09-21

### 新增

- **首次真实调用 Jev**：用 12 个关键词跑通 live 调用，产出 4 处可解释的分歧（见 `docs/01`）
- **剪贴板取数通道**（`src/clipboard_io.py`）：RPA 复制报表 → 流水线直接读，自动识别 TSV/CSV
- **分动作门控与硬约束落地**：无效花费概率 > 0.80 且动作是否定/暂停 → 强制转人工
- 示例输出 `examples/jev-live-run-sample.json`

### 修正（全部为实跑发现，纯看文档发现不了）

1. **`instructions` 缺判断口径** —— 只写了「按目标 ACOS 口径」而没说口径是什么，
   导致 Jev 对 12 个词里的 10 个都判「暂停」，连 ACoS 26% 的健康词也是，分歧率 91.7%。
   把六条判据显式写进 instructions 后，分歧率降到 **33.3%**、自动执行从 1 升到 7。
2. **`noul` 字段名解析错** —— 实际字段是 `noul`，代码读的是 `probability`，导致无效花费恒为 None。
3. **`score` 解析未兼容** —— 字段名在不同版本可能不同，改为 `score/level/value/rating` 逐个尝试。
4. **字段未透传** —— `Jev紧迫度` / `无效花费概率` 解析正确但没进 `Comparison.as_row()`，
   在中间环节被丢掉。属于"解析对了但数据丢了"，最难发现的一类。
5. **处置被覆盖** —— Jev 未接入时本应全部「待标定」，但代码继续落入高风险分支，
   把否定/暂停类动作改成了「人工复核」。改为未接入时提前返回。
6. **剪贴板编码串码** —— PowerShell 默认按 OEM 代码页（GBK/936）读写 stdin/stdout，
   中文写入后读回是乱码。须显式设 `[Console]::InputEncoding/OutputEncoding = UTF8`。
7. **剪贴板被覆盖** —— `--out-clipboard` 会冲掉用户剪贴板里的原有内容。
   现在写入前自动备份到 `output/clipboard-before-write-<时间戳>.txt`。
8. **网关 5xx 直接失败** —— 实测遇到 Cloudflare 520。对 5xx/429/超时加有限退避重试（3 次）。
9. **`.cmd` 调用长 JSON 失败** —— Windows 下 `.cmd` 经 cmd.exe 二次解析引号，
   几千字符的中文 JSON 会被拆坏。改用 npm 布局下的原生 `lark-cli.exe`。

### 变更

- `examples/output-sample.json` 更新为 12 词版本
- 飞书多维表格示例实例更新为真实 Jev 结果（4 条分歧 + 5 条待复核）

首个参考实现。

### 新增

- **流水线**：`state_builder` → `rule_layer` → `jev_shadow` → `compare` → `bitable_sync`
- **规则层**（`src/rule_layer.py`）：可追溯依据的硬阈值判断，与 `amazon-ads-optimization` 的 `ads_opt.mjs diagnose` 同源
- **Jev 影子层**（`src/jev_shadow.py`）：
  - 调用 OpenRouter decisions 端点（`typesafe/jev-1.13`）
  - 分动作门控阈值（加价 0.85 / 降价 0.70 / 暂停 0.90 / 保持 0.50 / 否定 0.75）
  - 硬约束：无效花费概率 > 0.80 时强制转人工
  - **默认 dry-run**：未标定时不联网，输出「未接入」
- **分歧检测**（`src/compare.py`）：区分「强度分歧」与「方向分歧」，产出处置建议
- **飞书多维表格同步**（`src/bitable_sync.py`）
- **文档**：定位与边界、分析载体设计、与上游项目的关系

### 设计决策

- **Jev 放在影子层而非主路径**：规则层可追溯，是核心资产；Jev 输出黑盒概率，放主路径会摧毁可复盘性
- **分歧检测只用「选中的答案」，不用置信度**：实测答案极稳（41 倍 state 变化下 10/10 不变），置信度漂 ±0.33
- **默认关闭 Jev**：置信度阈值必须在实际 state 形状上标定，未标定即启用会在两个方向上同时出错

### 修正

- **零单词的阈值退化**：单词无转化时 `1 ÷ CVR` 无法计算，原实现退化为阈值 0（等于不判断）。
  已改为**用品类 CVR 兜底**（全量订单 ÷ 全量点击），与独立实现的要求一致。
  实测：3.21% 品类 CVR → 阈值 33 次点击，`cheap blender for sale`（25 次）因此正确判为「样本不足」而非「否定」。

### 未完成

- **真实数据的影子标定**（`docs/01` 中列为硬前提）—— 未标定前 Jev 层不参与实际判断
- 多市场 / 多币种支持

## 版本号含义

- **MAJOR**：流水线结构或规则层判断逻辑发生不兼容变化
- **MINOR**：新增能力（新分析载体、新决策类型）或带标定数据调整阈值
- **PATCH**：文档与示例修正

