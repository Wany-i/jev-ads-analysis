# 安全政策

## 支持范围

本项目是一个**参考实现**，用于演示「决策模型如何参与广告优化分析」。
不包含服务端、不收集数据、**不执行任何广告写操作**。

## 设计上的安全边界

| 边界 | 说明 |
|---|---|
| **写操作仅限分析载体** | 唯一的写操作是把分析结果写入**飞书多维表格**（供人查看），且需显式去掉 `--dry-run` |
| **不碰广告账户** | 全仓库没有任何 Amazon Ads 的写接口调用 |
| **无凭据落盘** | `OPENROUTER_API_KEY` 从环境变量读；Base token 由命令行传参；均不写盘、不进日志 |
| **默认不联网** | `--jev dry-run` 为默认值；只有显式 `--jev live` 才发起外部调用 |
| **不做算术外传** | 所有派生指标在本地算出后才传入模型（既保隐私也保准确） |

## 已知的第三方依赖

| 依赖 | 用途 | 是否必需 |
|---|---|---|
| `lark-cli` | 同步到飞书多维表格 | 仅同步时需要 |
| `OPENROUTER_API_KEY` | 调用 Jev | 仅 `--jev live` 时需要 |

## 报告漏洞

请通过仓库 **Security → Report a vulnerability** 私密上报，不要开公开 Issue。
请附复现步骤、影响范围、建议修复方式。

## 使用者的责任

- 本项目输出的是**分析建议**，不是自动执行指令。**最终投放决策由使用者承担。**
- 请遵守 Amazon Ads 服务条款与当地法律法规。
- 影子测试使用的真实数据请自行脱敏，不要提交到仓库。
