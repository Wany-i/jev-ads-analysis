# 贡献指南

## 这个项目最需要什么

**本项目当前最大的缺口是真实数据的影子标定。** 因此最欢迎的贡献是：

1. **标定结果**：用你自己的真实数据跑影子对照，给出「多阈值下的准确率 / 覆盖率曲线」
2. **state 形状的改进**：哪些字段该加、哪些是噪声（注意：加字段会改变置信度分布，需重新标定）
3. **分歧案例的分析**：两层判断不一致时，人工判定谁更合理 —— 这是最有价值的反馈
4. **规则层的补充**：新的判断规则（必须带依据）

## 提交内容的要求

### 规则层改动

新增或修改判断规则时，必须说明：

- **依据**：出处（官方文档 / 独立实现 / 公开文章），并注明证据强度
- **是否多源**：单一来源的规则必须显式标注「单一来源」
- **优先接受可推导的规则，而不是固定魔数**
  - 例：`1 ÷ CVR`（可推导）优于「点击 15 次」（魔数）
  - 例：`ACoS > 1.15 × 盈亏平衡线`（相对值）优于「ACoS > 40%」（绝对值）

### Jev 层改动

- **不得**为了「效果更好」而放宽门控阈值而不附标定数据
- **不得**把算术交给模型（官方明确的失败模式 #2）
- **不得**往 state 里塞入判断不需要的字段（失败模式 #5：context rot）
- 阈值改动必须更新 `src/jev_shadow.py` 的 `GATE_THRESHOLDS` 并说明依据

## 不要提交

- ❌ 任何真实店铺数据、关键词、账号 ID（请脱敏）
- ❌ API Key、token、Cookie
- ❌ 任何 Amazon Ads 的写操作代码（本项目不做写操作）
- ❌ 未经标定的置信度阈值

## 代码约束

- **Python 3.10+，零第三方依赖**（标准库优先）
- 中文注释，关键判断处**写明依据出处**
- 改完请自测：

```bash
python src/pipeline.py --input examples/input-sample.csv \
  --price 39.99 --cost 8 --fba 7.5 --first-leg 3 \
  --commission-rate 15 --return-rate 5 --target-acos 25 --running-days 30 --bid 0.85 \
  --out output/pipeline-output.json
```

预期：7 行输入，全部落在「待标定」（Jev 未接入时应如此）。

## 文档风格

- 结论先行，再给依据
- 用表格承载规则与对照
- **不要把未验证的经验写成确定结论**
- 涉及「为什么这么设计」时，引用上游项目的实测数据而不是泛泛而谈
